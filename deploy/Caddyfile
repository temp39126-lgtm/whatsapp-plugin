{$DOMAIN} {
	encode gzip

	# API + WebSocket (Socket.io)
	handle /api/* {
		reverse_proxy backend:5000
	}

	handle /socket.io/* {
		reverse_proxy backend:5000
	}

	# Next.js frontend
	handle {
		reverse_proxy frontend:3000
	}
}
