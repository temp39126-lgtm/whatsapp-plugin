# Copy to keystore.properties and fill in values.
# Generate a keystore with: bash ../scripts/generate-keystore.sh

storeFile=release.keystore
storePassword=change-me
keyAlias=whatsappcrm
keyPassword=change-me
